import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hs.shuffle',
  appName: 'Shuffle: Random Color Generator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
